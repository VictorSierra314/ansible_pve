---
- name: Clean leaf packages
  block:
    - name: CLEANUP | Clean leaf rpm packages
      when: 
        - ansible_os_family == 'RedHat'
        - rpm_autorm_out is defined
      register: rpm_autorm_out
      ansible.builtin.yum:
        autoremove: true

    - name: CLEANUP | Clean leaf deb packages
      when: 
        - ansible_os_family == 'Debian'
        - deb_autorm_out is defined
      register: deb_autorm_out
      ansible.builtin.apt:
        purge: true
        autoremove: true

    - name: CLEANUP | Print autoremove output
      when:
        - (rpm_autorm_out | default({})).changed or (deb_autorm_out | default({})).changed
        - print_autorm_output | default('false') | bool
      ansible.builtin.debug:
        msg: |
          {% if ansible_os_family == 'RedHat' %}
          {{ rpm_autorm_out }}
          {% elif ansible_os_family == 'Debian' %}
          {{ deb_autorm_out.stdout_lines }}
          {% else %}
          OS familly not recognized
          {% endif %}

  rescue:
    - name: CLEANUP | Email notification on failure
      delegate_to: localhost
      community.general.mail:
        from: "{{ notification_sender }}"
        to: "{{ notification_email }}"
        host: "{{ smtp_host }}"
        port: "{{ smtp_port }}"
        username: "{{ smtp_user | default(omit) }}"
        password: "{{ smtp_password | default(omit) }}"
        attach: "{{ ansible_log.files[0].path | default(omit) }}"
        subject: "PVE VM Upgrade - ERROR - {{ inventory_hostname }} autoremove command FAILED"
        body: |
          ---------------------------
          {{ inventory_hostname }} - Upgrade FAILED
          ---------------------------
          Ansible FAILED to remove leaf packages in {{ inventory_hostname }}
          Date: {{ ansible_date_time.date }}
          Time: {{ ansible_date_time.time }} {{ ansible_date_time.tz }}
          Output:
          {% if ansible_os_family == 'RedHat' %}
          {{ rpm_autorm_out | to_nice_yaml }}
          {% elif ansible_os_family == 'Debian' %}
          {{ deb_autorm_out | to_nice_yaml }}
          {% else %}
          OS familly not recognized
          {% endif %}
